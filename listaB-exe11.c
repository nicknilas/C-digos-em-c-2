#include <stdio.h>
#include <stdlib.h>

float main (void)
{
	float N, R;
	printf("Digite o numero:");
	scanf("%f",&N);
	R=N*N;
	printf("O resultado do numero digitado ao quadrado e:%f", R);
	
	system("pause");
	return 0;
}
